<% if address.present? %>
  <%= image_tag "https://maps.googleapis.com/maps/api/staticmap?center=#{address.full_address.gsub(' ', '')}&zoom=15&size=500x250&maptype=roadmap&markers=color:red%7Clabel:Office%7C#{address.coordinates.join(',')}&key=#{ENV['GOOGLE_MAPS_API_KEY']}", alt: "#{address.office.name} Address" %>
<% end %>